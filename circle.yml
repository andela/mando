dependencies:
  pre:
    - bower install
  post:
    - npm install
test:
  override:
    - grunt test
    - protractor protractor.conf.js
deployment:
  production:
    branch: dev
    heroku:
      appname: andonation-mando