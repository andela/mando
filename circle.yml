dependencies:
  pre:
    - bower install
    - webdriver-manager update
test:
  override:
    - grunt test
    - protractor protractor.conf.js
deployment:
  production:
    branch: dev
    heroku:
      appname: andonation-mando