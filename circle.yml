machine:
  node:
    version:
      0.10.33
dependencies:
  pre:
    - bower install
test:
  pre:
    - grunt
    - sleep 5
  override:
    - grunt test
    - npm run protractor
deployment:
  production:
    branch: dev
    heroku:
      appname: andonation-mando